1. The program contains the main class FrequencyCalculator, where all the logic is stored + the method for getting letter frequencies is defined. This class has main method so it can be run for testing purposes.
2. The test text file is located in src/main/resources/companytasks/cub/ folder. The location is also defined in the FrequencyCalculator class and it should work out of the box.
3. Test is located in test/java/io/codelex/companytasks/cub package.